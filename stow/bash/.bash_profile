[ -s "$HOME/.bash_profile.default" ] && . "$HOME/.bash_profile.default"
[ -s "$HOME/.profile.default" ] && . "$HOME/.profile.default"

# This made bashrc load twice
# if [ -f ~/.bashrc ]; then
# 	. ~/.bashrc
# fi
