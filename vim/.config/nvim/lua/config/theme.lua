require('github-theme').setup({
  dark_float = true,
  hide_inactive_statusline = false,
  sidebars = {'packer', 'telescope'},
  theme_style = 'dark',
  transparent = true,
  comment_style = "NONE",
  keyword_style = "NONE"
})

-- TODO match colors or gitsigns to the theme?
-- nvim tree color when picking a window A B
-- depending on the theme, tmux blink should change color
-- work on dark gtk themes
-- different color for classes and constants
