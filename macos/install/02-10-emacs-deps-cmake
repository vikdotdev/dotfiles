#!/bin/bash

set -euo pipefail

source "$DOT_HOME/lib/macos/include.sh"

log_step "Installing cmake"

if brew_installed cmake; then
    log_info "cmake already installed"
else
    install_formula cmake
fi

log_success "cmake dependency installed"
