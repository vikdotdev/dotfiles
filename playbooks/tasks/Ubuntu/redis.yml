- name: Install redis packages
  become: yes
  apt:
    name:
    - redis

- name: Start database services
  become: yes
  ansible.builtin.systemd:
    name: redis
    enabled: yes
    state: started
