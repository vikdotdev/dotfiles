name: home
root: ~/

attach: true

windows:
  - init: tt; tt list
  - dotfiles: cd <%= ENV['EXT_XDG_REPOS_DIR'] %>/dotfiles; nvim
  - notes: cd <%= ENV['EXT_XDG_REPOS_DIR'] %>/notes; nvim
  - practice: cd <%= ENV['EXT_XDG_REPOS_DIR'] %>/practice; nvim
  - todo: task context list
  - ssh:
